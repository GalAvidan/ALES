{
  "_meta": {
    "generated_at": "2026-04-24T10:00:00Z",
    "ttl_days": 7,
    "stale": false,
    "stale_reason": null,
    "derived_from": ["src/timeline/", "src/assets/"],
    "fingerprint": "sha256:placeholder"
  },
  "flow_id": "scene-render",
  "description": "From scene script intent to a rendered output file",
  "steps": [
    {
      "step": 1,
      "action": "load",
      "target": "map/scenes.json",
      "purpose": "Get scene duration, asset refs, audio sync points for the target scene"
    },
    {
      "step": 2,
      "action": "load",
      "target": "map/assets.json",
      "purpose": "Resolve asset file paths and validate they exist"
    },
    {
      "step": 3,
      "action": "invoke_skill",
      "skill": "easing-style",
      "purpose": "Apply default easing to all motion unless the scene has a motion_override"
    },
    {
      "step": 4,
      "action": "invoke_skill",
      "skill": "audio-sync",
      "purpose": "Align animation keyframes to declared audio_sync_points"
    },
    {
      "step": 5,
      "action": "invoke_skill",
      "skill": "color-grade",
      "purpose": "Apply the project LUT to the rendered output"
    },
    {
      "step": 6,
      "action": "emit",
      "output_schema": "render-spec",
      "purpose": "Produce a structured render specification ready for the render pipeline"
    }
  ]
}
